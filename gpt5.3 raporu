# GPT-5.3 Raporu (Derin İnceleme - Revizyon 2)

Tarih: 2026-03-04  
İnceleme tipi: Kod değiştirmeden tam-repo denetim (mimari + güvenlik + mantık + test)  
İncelenen kök: `/home/yusuf/ai-agent (diğer kopya)`

## 1) Kapsam ve Yöntem

Bu revizyonda önceki rapora ek olarak:
1. Kritik modüller satır bazlı manuel incelendi (`agent.py`, `web_ui.py`, `api_server.py`, `dashboard.py`, `services/agent_service.py`, `swarm/*`, `utils/*`, `deep_learning.py`, `rag_engine.py`, `xai_engine.py`).
2. Hedefli runtime doğrulamaları yapıldı (küçük `python3` snippet’leri ile hataların tetiklenebilirliği test edildi).
3. Test paketi tekrar çalıştırıldı ve kırık noktalar yeniden doğrulandı.
4. Güvenlik odaklı pattern taraması yapıldı (`path`, `subprocess`, `pickle/joblib`, zayıf auth, global state).

## 2) Çalıştırılan Doğrulamalar

1. `pytest tests/ -q`
- Sonuç: `4 failed, 344 passed, 4 skipped`
- Kırık testler:
  - `tests/test_agent.py::TestIsDangerousBash::test_blocks_dd_devzero`
  - `tests/test_api_contract.py::test_train_cnn_endpoint`
  - `tests/test_xai.py::test_generate_shap_summary`
  - `tests/test_xai.py::test_explain_instance_lime`

2. Hedefli runtime doğrulamaları
- `SwarmOrchestrator(AgentConfig(...))` çağrısı `AttributeError: 'PosixPath' object has no attribute 'base_dir'` üretiyor.
- `DataPreprocessor().summary_text()` çağrısı `ValueError: Unknown format code '%' for object of type 'str'` üretiyor.
- `is_dangerous_bash("rm -fr /")` ve `is_dangerous_bash("dd if=/dev/zero of=/dev/sda")` engellemeden geçiyor.
- `ModelComparator.save_all_models()` ile kaydedilen modeller fit’siz pipeline’lar (beklenen "eğitilmiş" davranışı yok).

## 3) Önceliklendirilmiş Bulgular

## P0 (Kritik)

### P0-1) Swarm modu servis katmanında fiilen kırık (tip uyumsuzluğu)

- Kanıt:
  - `SwarmOrchestrator` `cfg.workspace.base_dir` bekliyor: `swarm/orchestrator.py:15`
  - `AgentService`/`agent.py` tarafında kullanılan config’te `workspace` bir `Path`: `agent.py:859-864`, `services/agent_service.py:101-107`
- Runtime doğrulama:
  - `SwarmOrchestrator(AgentConfig(...))` -> `AttributeError`
- Etki:
  - Web UI / servis tarafında swarm devreye alındığında hata verip fallback’e düşüyor; vaat edilen mimari pratikte çalışmıyor.
- Öneri:
  - `SwarmOrchestrator` hem `AppConfig` hem `AgentConfig` destekleyecek normalize katmanı kullanmalı.

### P0-2) Dashboard model-info endpoint’inde path + unsafe deserialize riski

- Kanıt:
  - İstekten gelen path doğrudan birleştiriliyor: `dashboard.py:770-776`
  - Model bilgisi almak için `joblib.load` yapılıyor: `utils/model_loader.py:44`
- Etki:
  - Path traversal/absolute path ile workspace dışı dosyalar hedeflenebilir.
  - Güvenilmeyen `.pkl` yüklemesi ile code execution riski oluşur.
- Öneri:
  - `resolve()` + `relative_to(BASE_DIR/workspace)` sınır kontrolü.
  - Model introspection için güvenli metadata-only yaklaşımı, `joblib.load`’u doğrulanmamış dosyada yasaklama.

### P0-3) Multimodal konuşma geçmişi kayıt/yükleme akışında çökme

- Kanıt:
  - `save_conversation` kullanıcı içeriğini string varsayıyor: `agent.py:896`
  - Dosya ekli mesaj içeriği list olarak oluşturuluyor: `services/agent_service.py:291-297`
  - Oturum yükleme sırasında da `startswith` string varsayımı var: `web_ui.py:481-483`
- Etki:
  - Dosya/medya içeren gerçek kullanıcı akışlarında history save/load kararsız.
- Öneri:
  - Mesaj içeriği için tip güvenli serialize/deserialize (str/list/dict) standardı.

### P0-4) Bash güvenlik filtreleri hem hatalı hem kolay bypass edilebilir

- Kanıt:
  - Varsayılan denylist’te typo var (`dd` yerine `ddd`): `agent.py:283`
  - `rm -fr /` varyantı regex’i bypass ediyor (`-rf` sırasına bağımlı): `agent.py:281,301-307`
  - Kırık test bunu doğruluyor: `tests/test_agent.py:70`
- Etki:
  - Yüksek riskli komutlar bloklanmadan çalışabilir.
- Öneri:
  - Regex tabanlı denylist tek başına yeterli değil; allowlist + komut parser + sandbox kombinasyonuna geçilmeli.

### P0-5) Çoklu kullanıcı izolasyonu zayıf (global state + ortak bellek)

- Kanıt:
  - Web UI’de global singleton servis: `web_ui.py:31-37`
  - Proje bağlamı process-level env’e yazılıyor: `services/agent_service.py:172`; okunuyor: `agent.py:330-334`
  - Bellek sorguları session filtresiz: `memory_manager.py:111-113`; çağrı da filtersiz: `services/agent_service.py:287-289`
- Etki:
  - Oturumlar arasında proje/bağlam sızıntısı, yanlış dosyaya yazma, yanlış hafıza enjeksiyonu.
- Öneri:
  - Session-scoped state; env yerine instance state; memory lookup’ta session bazlı filtreyi default yap.

### P0-6) Varsayılan dağıtımda güçlü auth olmadan dışa açılabilen yürütme yüzeyleri

- Kanıt:
  - Web UI `0.0.0.0` bind: `web_ui.py:566`
  - WhatsApp connector `0.0.0.0` bind: `whatsapp_connector.py:192`
  - API key default boş (`security off`): `utils/config.py:69`, `api_server.py:88-91`
- Etki:
  - Uygulama ağda yayınlandığında yetkisiz tetikleme/riskli işlem yüzeyi büyüyor.
- Öneri:
  - Secure-by-default: zorunlu API key/JWT, localhost default bind, prod profile’da explicit allowlist.

### P0-7) Tool parse/execute akışında multi-tool mantık bozulması

- Kanıt:
  - Parser tag bazlı döndüğü için metin sırası korunmuyor: `agent.py:1028-1062`
  - Tool sonrası çıktı işleme blokları loop dışında; pratikte son tool’a yapışıyor: `agent.py:1493-1633`, kritik `1605+`
- Etki:
  - Çoklu tool cevaplarında yanlış sıra, eksik kayıt, yanlış continue prompt.
- Öneri:
  - Position-aware tek-pass parser + loop kapsamının net ayrıştırılması.

## P1 (Yüksek)

### P1-1) Redis yokken API kontratı bozuluyor (500)

- Kanıt:
  - `enqueue` çağrıları doğrudan: `api_server.py:123,148,173`
  - Testte `ConnectionError` ile 500: `tests/test_api_contract.py:20`
- Etki:
  - Sistem dış bağımlılık yokluğunda kontrollü degrade olamıyor.
- Öneri:
  - `ConnectionError` yakalayıp `503` + tutarlı JSON sözleşmesi döndür.

### P1-2) RAG_SEARCH entegrasyonu parser/çıktı sözleşmesi açısından kırık

- Kanıt:
  - Prompt’ta `RAG_SEARCH` var: `agent.py:193-198`
  - Parser tag listesinde yok: `agent.py:269`
  - Çalıştırma branch’i var: `agent.py:1536`
  - Kod `distance` bekliyor: `agent.py:1544`; engine `score` dönüyor: `rag_engine.py:406-411`
- Etki:
  - Özellik kısmen erişilemez, erişilse bile key mismatch ile kırılabilir.

### P1-3) Path boundary eksikleri (Data Explorer)

- Kanıt:
  - Kullanıcı path’i `work_dir / filepath` ile birleştiriliyor; `resolve+relative_to` yok: `web_ui.py:355-358`
- Etki:
  - Crafted relatif/absolute path ile workspace dışı dosya okuma riski.

### P1-4) Screenshot dosya yollarında sanitize yok

- Kanıt:
  - `browser_action` screenshot path doğrudan kullanılıyor: `agent.py:618-626`
  - Browser sub-agent’da aynı problem: `browser_agent.py:200-205`
- Etki:
  - Workspace dışına yazma/path overwrite riski.

### P1-5) `DataPreprocessor.summary_text()` varsayılan kullanımda patlıyor

- Kanıt:
  - `%` formatı string fallback ile kullanılıyor: `utils/preprocessor.py:344-345`
  - Runtime doğrulama: `ValueError`
- Etki:
  - Raporlama/özet UI/CLI akışları bozuluyor.

### P1-6) `save_all_models` eğitilmiş model değil, template pipeline kaydediyor

- Kanıt:
  - Döngü `self.models` (orijinal model sözlüğü) üzerinde: `utils/model_compare.py:557-563`
- Etki:
  - "tüm eğitilmiş modeller" beklentisi karşılanmıyor; üretim kullanımı hatalı sonuç verir.

### P1-7) Rapor sonucu metriği regresyonda mantıksal olarak yanlış seçilebilir

- Kanıt:
  - Sonuç cümlesi için `max(metrics.items())` kullanılıyor: `report_generator.py:323-326`
- Etki:
  - MAE/RMSE gibi düşük olması gereken metriklerde yanlış “en iyi” vurgusu üretilebilir.

### P1-8) Kalıcı bellek tasarımı workspace/session sınırında izole değil

- Kanıt:
  - DB yolu sabit repo altına kitli: `memory_manager.py:14-16`
  - `get_context_string()` session filtresi kullanmıyor: `memory_manager.py:111-113`
- Etki:
  - Farklı çalışma alanı/oturumlar arasında bağlam bulaşması.

### P1-9) XAI test/bağımlılık matrisi CI ile uyumsuz

- Kanıt:
  - Testler SHAP/LIME yoksa skip etmiyor: `tests/test_xai.py:31-54`
  - CI sadece `.[test]` kuruyor: `.github/workflows/test.yml:28`
  - SHAP/LIME `xai` extra’da: `pyproject.toml:99-103,106-110`
- Etki:
  - CI/yerel ortam farkı kaynaklı kırık testler.

### P1-10) Örnek config şema ile çelişiyor

- Kanıt:
  - `config.example.yaml` `max_steps: 9999`: `config.example.yaml:10`
  - Pydantic sınırı `<=100`: `utils/config.py:64`
- Etki:
  - Örnek dosya doğrudan kullanıldığında validation hatası.

### P1-11) Dashboard config update uç noktası şemasız yazıyor

- Kanıt:
  - `deep_update` ile doğrulamasız merge: `dashboard.py:803-819`
- Etki:
  - Geçersiz tip/değerler runtime’da başka modülleri kırabilir.

## P2 (Orta)

### P2-1) API sunucusu doğrudan çalıştırmada `reload=True`

- Kanıt: `api_server.py:230`
- Etki:
  - Geliştirme dışı kullanımda gereksiz watcher/CPU/log yükü.

### P2-2) CLI RAG çıktısı key uyumsuzluğu nedeniyle patlayabilir

- Kanıt:
  - CLI `/rag` yazdırırken `distance` bekliyor: `agent.py:1356`
  - `rag.search()` `score` döndürüyor: `rag_engine.py:406-411`

### P2-3) SQL sorgu oluşturma f-string ile (enjekte edilebilir tasarım)

- Kanıt:
  - `get_new_data_since`: `data_streams/db_connector.py:51`
  - `mark_as_analyzed`: `data_streams/db_connector.py:63`
- Etki:
  - Fonksiyonlar dış girdiye açıldığında SQL injection riski.

### P2-4) Vault güvenlik iddiası ile implementasyon çelişkili

- Kanıt:
  - README "şifreli kasa" diyor: `README.md:93`
  - Gerçekte düz JSON yazılıyor: `utils/vault.py:23-27,34-44`
- Etki:
  - Kimlik bilgileri diskte plaintext.

### P2-5) Teknik borç: duplicate importlar / placeholder kod

- Kanıt:
  - `agent.py` içinde duplicate import blokları: `agent.py:18-20,33`
  - Placeholder helper’lar üretimde kalmış: `agent.py:1087-1095`
- Etki:
  - Okunabilirlik ve bakım maliyeti artıyor.

### P2-6) Repo hijyeni

- Kanıt:
  - Büyük log dosyaları root’ta tracked (`api_server.log`, `job_worker.log`).
  - `.gitignore` yalnız `logs/*.log` ignore ediyor: `.gitignore:29-31`
  - Self-referential editable git dependency: `requirements.txt:16`
- Etki:
  - Repo şişmesi, reproducibility ve güvenlik taraması gürültüsü.

### P2-7) Test organizasyonunda dağınıklık

- Kanıt:
  - Kökte ek `test_xai.py` mevcut: `test_xai.py:1-47`
  - `tests/test_xai.py` ile amaç çakışması: `tests/test_xai.py:1-54`
- Etki:
  - Çalıştırma komutuna göre farklı kırık test sayısı ve belirsizlik.

## P3 (İyileştirme / “Olsa Daha İyi Olur”)

1. `ruff + mypy + pre-commit` ile statik kalite barajı.
2. Güvenlik pipeline’ına `bandit`, dependency audit, secret scan eklenmesi.
3. Endpoint’lerde structured error code sözleşmesi (ör. `code`, `message`, `hint`).
4. Oturum ve görevler için açık yaşam döngüsü metrikleri (queue wait, run time, fail reason).
5. “Prod profile” (`reload=False`, localhost bind default, zorunlu auth) ayrı konfig ile netleştirme.

## 4) Güçlü Yönler (Doğrular)

1. Geniş bir test tabanı var (`344` geçen test sağlam bir temel).
2. `exceptions.py` hata modeli ve kullanıcıya dönük mesaj katmanı iyi ayrılmış.
3. LLM backend soyutlaması (`llm_backend.py`) farklı sağlayıcılar için doğru bir mimari yön.
4. Modülerlik iyi: API, UI, worker, swarm, RAG, ML yardımcıları birbirinden ayrılmış.
5. Dokümantasyon hacmi yüksek ve geliştirici onboarding’i güçlü.

## 5) Net Aksiyon Planı

### İlk 24 Saat
1. `P0-1`, `P0-3`, `P0-4`, `P0-7` düzeltmeleri (swarm init, konuşma tip güvenliği, denylist, tool loop).
2. `P1-1` için Redis degrade davranışı (`503` + net hata modeli).
3. `P1-10` için `config.example.yaml` şema hizası.

### İlk 72 Saat
1. Dashboard ve Data Explorer path boundary hardening (`resolve + relative_to`).
2. Unsafe pickle load yüzeyini kapatma (`P0-2`).
3. `P1-5`, `P1-6`, `P1-7` (preprocessor/report/model_compare doğruluk düzeltmeleri).
4. XAI testlerini dependency-aware yapmak veya CI’ye `.[test,xai]` profili eklemek.

### İlk 1 Hafta
1. Session izolasyon refactor’u (global singleton/env bağımlılığını kaldırma).
2. Bellek (RAG) izolasyon stratejisi: workspace+session scoped indeksleme.
3. Secure-by-default deployment profili ve auth zorunluluğu.
4. Repo hijyeni ve bağımlılık sadeleştirme.

## 6) Sonuç

Kod tabanı güçlü bir iskelete sahip; ancak üretim güvenliği ve deterministik davranış için birkaç kritik düğüm aynı anda kapanmalı.

Kısa hüküm:
1. Mimari yön doğru.
2. P0 seviyesinde hem güvenlik hem çalışma-zamanı kararlılığı etkileyen somut açıklar var.
3. Bu rapordaki ilk 7 kritik madde kapatıldığında sistemin güvenilirliği belirgin şekilde artar.

---

## 7) Adım Adım Uygulama Rehberi (Tam Liste)

Bu bölüm “her şeyi adım adım” kapatmak için hazırlanmış yürütme sırasıdır.  
Her adımda: ne yapılacak, nasıl doğrulanacak ve başarı kriteri belirtilmiştir.

### Faz 0 - Güvenli Başlangıç

1. Çalışma dalı aç (`audit-fixes-phase1`).
2. Mevcut test durumunu baseline al (`pytest tests/ -q` çıktısını sakla).
3. Değişiklikleri küçük commit parçalarına bölme kuralını belirle (her P0/P1 maddesi ayrı commit).

### Faz 1 - P0 Kritikler

4. `swarm/orchestrator.py` içinde config normalizasyon katmanı ekle.
5. `cfg.workspace.base_dir` varsayımını kaldır; `Path` ve `AppConfig` iki tipini de destekle.
6. `AgentService` üzerinden swarm toggle akışını tekrar dene.
7. Doğrulama: `SwarmOrchestrator(AgentConfig(...))` çağrısı hatasız açılmalı.
8. Başarı kriteri: `AttributeError` tamamen kalkmalı.

9. `dashboard.py:/api/models/info` endpoint’inde path boundary kontrolü ekle.
10. `resolve()` + `relative_to(BASE_DIR / "workspace")` zorunluluğu getir.
11. Absolute ve traversal path’leri `400/403` ile reddet.
12. Doğrulama: `path='../../etc/passwd'` ve `path='/etc/passwd'` reddedilmeli.
13. Başarı kriteri: workspace dışı model dosyası asla okunmamalı.

14. `utils/model_loader.py` içinde unsafe `joblib.load` yüzeyini sınırla.
15. Sadece güvenilir dizin + uzantı kontrolü geçerse yükleme yap.
16. Mümkünse endpoint tarafında model yüklemeden sadece `_meta.json` okuyan güvenli mod ekle.
17. Doğrulama: sahte/zararlı pickle dosyasıyla endpoint testinde yürütme olmamalı.
18. Başarı kriteri: deserialize sadece izinli patikada çalışmalı.

19. `agent.py:save_conversation` için içerik tip normalizasyonu ekle.
20. `str/list/dict` mesaj içeriklerinin güvenli özetleme fonksiyonunu yaz.
21. `web_ui.py:on_load_session` içinde `startswith` öncesi tip kontrolü ekle.
22. Doğrulama: dosya ekli multimodal geçmiş kaydet/yükle akışı hatasız çalışmalı.
23. Başarı kriteri: `AttributeError` tamamen kalkmalı.

24. `agent.py` denylist typo’yu düzelt (`ddd` -> `dd`).
25. Regex’i `rm -rf/-fr` varyantlarını da kapsayacak şekilde sertleştir.
26. Gerekirse komut tokenizasyonu ile sadece regex’e bağımlılığı azalt.
27. Doğrulama: `tests/test_agent.py::test_blocks_dd_devzero` yeşil olmalı.
28. Ek doğrulama: `is_dangerous_bash("rm -fr /")` da bloklanmalı.
29. Başarı kriteri: kritik destructive komut varyantları engellenmeli.

30. `web_ui.py` global singleton servis kullanımını session-scope’a geçir.
31. `services/agent_service.py` içindeki `AGENT_PROJECT` env kullanımını instance state ile değiştir.
32. `memory_manager` çağrılarında session bazlı filtreyi default hale getir.
33. Doğrulama: iki eşzamanlı oturumda proje klasörü/hafıza sızıntısı olmamalı.
34. Başarı kriteri: kullanıcılar arası state izolasyonu sağlanmalı.

35. Dışa açılan servislerde secure-by-default ayarlarını uygula.
36. API key boşsa kritik endpoint’leri kapalı moda al veya startup’ta uyarı+fail ver.
37. `web_ui.py` ve `whatsapp_connector.py` için production bind stratejisini ayır (`127.0.0.1` default).
38. Doğrulama: auth’suz uzak çağrıların bloklandığı test ekle.
39. Başarı kriteri: açık ağda auth’suz kritik işlem tetiklenememeli.

40. `extract_tools` fonksiyonunu sıralı (position-aware) parser ile değiştir.
41. `agent.py` tool loop kapsamını düzelt; per-tool output handling loop içinde kalmalı.
42. Multi-tool response için deterministik yürütme sırasını garanti et.
43. Doğrulama: `<BASH><PYTHON><BASH>` benzeri senaryolarda sıra korunmalı.
44. Başarı kriteri: son tool’a yapışma bug’ı kalkmalı.

### Faz 2 - P1 Yüksekler

45. `api_server.py` enqueue noktalarına `ConnectionError` yakalama ekle.
46. Redis yoksa tutarlı `503` JSON sözleşmesi döndür.
47. Doğrulama: `tests/test_api_contract.py::test_train_cnn_endpoint` 202/403/503 kabulüne çekilmeli.
48. Başarı kriteri: Redis yokluğunda 500 kalkmalı.

49. `RAG_SEARCH` tag’ini parser tool listesine ekle.
50. RAG çıktı sözleşmesini tekilleştir (`score` veya `distance` birini seç, her yerde aynı kullan).
51. CLI `/rag` çıktısını yeni sözleşmeye hizala.
52. Doğrulama: CLI ve tool path’te aynı key ile sonuç yazdırılmalı.
53. Başarı kriteri: RAG branch’inde key mismatch kalmamalı.

54. Data Explorer preview’de path doğrulaması ekle.
55. `full_path = (work_dir / filepath).resolve()` + `relative_to(work_dir.resolve())` zorunlu kıl.
56. Doğrulama: traversal girişleri reddedilmeli.
57. Başarı kriteri: workspace dışı okuma engellenmeli.

58. Screenshot path’lerini sanitize et (`safe_relpath` benzeri kontrol).
59. Hem `browser_action` hem `browser_agent` için ortak güvenli yazma fonksiyonu kullan.
60. Doğrulama: `../` içeren screenshot adı reddedilmeli.
61. Başarı kriteri: workspace dışı yazma kapanmalı.

62. `utils/preprocessor.py` summary format hatasını düzelt.
63. `pca_explained_variance` yokken `%` formatına girmeyecek güvenli fallback ver.
64. Doğrulama: `DataPreprocessor().summary_text()` exception atmamalı.
65. Başarı kriteri: rapor özeti her durumda üretilebilmeli.

66. `save_all_models` fonksiyonunu gerçekten eğitilmiş modelleri kaydedecek hale getir.
67. Eğitim sırasında fit edilmiş model kopyalarını state’te tut (`trained_models` gibi).
68. `save_all_models` bu fit edilmiş nesneleri dump etsin.
69. Doğrulama: dump edilen LogisticRegression içinde `coef_` gibi fit attribute’ları bulunmalı.
70. Başarı kriteri: “tüm model kaydet” semantiği doğru çalışmalı.

71. `report_generator.py` sonuç cümlesindeki metrik seçimini düzelt.
72. Görev tipine göre optimize yönü belirle (`accuracy/f1/roc_auc`: yüksek iyi, `mae/rmse`: düşük iyi).
73. Doğrulama: regresyon raporunda yanlış metrik “en iyi” seçilmemeli.
74. Başarı kriteri: sonuç paragrafı metrik mantığıyla uyumlu olmalı.

75. `memory_manager.py` DB yolunu config/workspace ile uyumlu hale getir.
76. Session filtresini `get_context_string` içine parametreli olarak zorunlu kullan.
77. Doğrulama: farklı session’larda cross-memory gelmemeli.
78. Başarı kriteri: bağlam izolasyonu.

79. XAI test stratejisini düzelt.
80. `tests/test_xai.py` içinde `pytest.importorskip("shap")` ve `pytest.importorskip("lime")` uygula veya marker kullan.
81. CI’da `.[test,xai]` profiline geçiş kararını netleştir.
82. Doğrulama: CI deterministic olmalı (ortama bağlı kırık kalmamalı).
83. Başarı kriteri: XAI testleri ya skip ya pass, fail etmemeli.

84. `config.example.yaml` değerlerini Pydantic sınırlarıyla hizala.
85. `max_steps` örneğini `<=100` değerine çek.
86. Doğrulama: örnek config ile `load_config` validation hatasız geçmeli.
87. Başarı kriteri: örnek dosya çalışır referans olmalı.

88. Dashboard config update endpoint’i için şema doğrulaması ekle.
89. Gelen payload’ı `AppConfig` üzerinden doğrula, invalid alanı reddet.
90. Doğrulama: yanlış tipte değer gönderimi `400` dönmeli.
91. Başarı kriteri: runtime config bozulmaları engellenmeli.

### Faz 3 - P2 Orta Seviye

92. `api_server.py` doğrudan çalıştırmada `reload=False` yap.
93. Development/prod ayarını env/profile ile ayır.
94. Doğrulama: prod başlatmada autoreload kapalı olmalı.

95. `data_streams/db_connector.py` sorgularını parametreli hale getir.
96. SQLAlchemy text+bind parametreleri kullan.
97. Doğrulama: injection karakterleri içeren inputlarda query güvenli çalışmalı.

98. `utils/vault.py` için gerçek güvenlik seviyesi kararı al.
99. Ya README’de plaintext olduğu açıkça yaz, ya dosyayı şifrele (örn. keyring/fernet).
100. Doğrulama: dokümantasyon-uygulama tutarlılığı sağlanmalı.

101. `agent.py` duplicate import ve placeholder kod temizliği yap.
102. Kullanılmayan/yanıltıcı mock helper’ları kaldır.
103. Doğrulama: lint ve testler etkilenmeden geçmeli.

104. Repo hijyeni düzenlemesi yap.
105. Büyük log dosyalarını repo dışına taşı; `.gitignore` kuralını genişlet.
106. `requirements.txt` içindeki self-referential editable satırı kaldır veya dokümante et.
107. Doğrulama: temiz clone sonrası gereksiz artifact izlenmemeli.

108. Test organizasyonunu sadeleştir.
109. Kökteki `test_xai.py` dosyasını `tests/` altına taşı veya net bir amaçla işaretle.
110. Doğrulama: `pytest -q` ve `pytest tests/ -q` beklenti farkı dokümante edilmeli.

### Faz 4 - P3 Kalite ve Operasyonel Sertleştirme

111. `ruff + mypy + pre-commit` hattını ekle.
112. Güvenlik taramasına `bandit` ve dependency audit ekle.
113. Endpoint error contract’ını standartlaştır (`code`, `message`, `hint`).
114. Queue/worker/agent için gözlemlenebilirlik metriklerini genişlet.
115. Prod profile dokümantasyonu oluştur (`bind`, `auth`, `reload`, `rate limit`).

### Faz 5 - Kapanış ve Regresyon Kontrolü

116. Tüm testleri çalıştır (`pytest tests/ -q`).
117. Kritik akışlar için smoke test yap:
118. Swarm açık/kapalı.
119. Multimodal mesaj save/load.
120. Redis var/yok API davranışı.
121. Path traversal negatif testleri.
122. RAG_SEARCH tool akışı.
123. Son raporu güncelle: kapanan maddeler, kalan riskler, teknik borç listesi.

### Faz 6 - Teslimat Kriterleri

124. P0 maddelerinin tamamı kapatılmadan production release yapılmamalı.
125. P1 maddeleri kapandıktan sonra güvenli beta etiketi verilebilir.
126. P2/P3 maddeleri planlı sprint backlog’a aktarılıp tarih atanmalı.

---

## Ek A) Referans Satır İndeksi

- `agent.py:269,281-283,301-307,330-334,618-626,859-864,886-897,1028-1062,1356,1493-1633,1536,1544,1605`
- `services/agent_service.py:101-107,172,287-297`
- `web_ui.py:31-37,355-358,481-483,566`
- `dashboard.py:770-776,803-819`
- `api_server.py:88-91,123,148,173,230`
- `swarm/orchestrator.py:15`
- `browser_agent.py:200-205`
- `rag_engine.py:406-411`
- `memory_manager.py:14-16,111-113`
- `utils/model_loader.py:44`
- `utils/preprocessor.py:344-345`
- `utils/model_compare.py:557-563`
- `report_generator.py:323-326`
- `utils/config.py:64,69`
- `config.example.yaml:10`
- `tests/test_agent.py:70`
- `tests/test_api_contract.py:20`
- `tests/test_xai.py:31-54`
- `.github/workflows/test.yml:28`
- `pyproject.toml:99-103,106-110`
- `data_streams/db_connector.py:51,63`
- `utils/vault.py:23-27,34-44`
- `README.md:93`
- `.gitignore:29-31`
- `requirements.txt:16`

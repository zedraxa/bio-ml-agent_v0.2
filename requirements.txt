# ═══════════════════════════════════════════════════════════
#  Bio-ML Agent — Ana Proje Bağımlılıkları
#  Kurulum: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════

# ── Çekirdek (Core) ──────────────────────────────────────
ollama>=0.4.0            # Yerel LLM backend (Ollama API)
pyyaml>=6.0              # Yapılandırma dosyası (config.yaml) desteği

# ── Web Araçları ─────────────────────────────────────────
requests>=2.28.0         # HTTP istekleri (web_open)
beautifulsoup4>=4.11.0   # HTML parse (web_open)
duckduckgo-search>=7.0.0 # Web araması (web_search / ddgs)

# ── Vektör Veritabanı & RAG ──────────────────────────────
chromadb>=0.4.0          # Yerel vektör DB (RAG aramaları)
sentence-transformers>=2.2.0 # ChromaDB yerel embedding oluşturucu

# ── ML / Veri Bilimi ─────────────────────────────────────
scikit-learn>=1.2.0      # Makine öğrenmesi modelleri
pandas>=1.5.0            # Veri işleme (DataFrame)
numpy>=1.24.0            # Sayısal hesaplama
matplotlib>=3.6.0        # Görselleştirme (model karşılaştırma grafikleri)
seaborn>=0.12.0          # İleri düzey istatistiksel görselleştirme
plotly>=5.15.0           # Etkileşimli dinamik grafikler (Rapor ve UI için)

# ── Test / Geliştirme ────────────────────────────────────
pytest>=7.0.0            # Unit test framework

# ── Web Arayüzleri ──────────────────────────────────────
flask>=3.0.0             # Dashboard web paneli
flask-cors>=4.0.0        # REST API CORS desteği
gradio>=4.40.0,<5.0.0    # Gradio chat arayüzü (Gradio 4 serisi)

# ── Bulut LLM API (Remote Backend Desteği) ──
openai>=1.0.0          # OpenAI API (GPT-4, GPT-4o vb.)
anthropic>=0.30.0      # Anthropic API (Claude 3.5 vb.)
google-genai>=0.5.0    # Google Gemini API
twilio>=8.0.0          # WhatsApp entegrasyonu

# ── Derin Öğrenme (V6 — Deep Learning) ───────────────────
torch>=2.0.0              # PyTorch DL framework
torchvision>=0.15.0       # Pretrained CNN modelleri (ResNet, EfficientNet vb.)
tqdm>=4.65.0              # Eğitim progress bar
Pillow>=9.0.0             # Görüntü yükleme (PIL)

# ── AutoML (Opsiyonel — Neural Architecture Search) ──────
# ── AutoKeras + TensorFlow (ağır, sadece NAS için):
# autokeras>=1.1.0       # Neural Architecture Search
# tensorflow>=2.13.0     # AutoKeras backend

# ── Açıklanabilir Yapay Zeka (XAI) ───────────────────
shap>=0.42.0              # SHAP (SHapley Additive exPlanations)
lime>=0.2.0.1             # LIME (Local Interpretable Model-agnostic Explanations)

# ── Kurumsal API (FastAPI) ───────────────────────────
fastapi>=0.100.0          # REST API Framework
uvicorn>=0.23.0           # ASGI Web Server
python-multipart>=0.0.6   # Dosya yükleme desteği
